<!--
WORKSPACE SETUP: [Goal Name]
Target: [Environment]
Generated by: setup-prompt-forge

PREREQUISITES:
  - [Prerequisite 1]
  - [Prerequisite 2]

RESUMING: Re-paste this prompt if interrupted. Completed phases will be skipped.
-->

# [Goal Name] — Workspace Setup

You are setting up [brief description of what this achieves].

---

## Step 0: Initialize State

Check if `.setup/state.json` exists.

If it does, read it and parse `completed_phases`. You will skip any phase whose ID appears in this list.

If it does not exist, create the `.setup/` directory and write:

```json
{
  "goal": "[goal name]",
  "started_at": "[current ISO timestamp]",
  "last_updated": "[current ISO timestamp]",
  "completed_phases": [],
  "values": {},
  "errors": []
}
```

---

## Phase 0: Preflight Checks [ID: preflight]

Skip if "preflight" is in `completed_phases`.

Check each item below. If any fail, print the error and stop — do not continue to Phase 1.

1. [Check 1] — run `[command]` and verify [expected result]
2. [Check 2] — verify `$[ENV_VAR]` is set and non-empty
3. [Check 3]

If all pass:
- Update `state.json`: append "preflight" to `completed_phases`, set `last_updated`
- Print: "✓ Preflight passed"

---

## Phase 1: [Phase Name] [ID: phase-1]

Skip if "phase-1" is in `completed_phases`.

1. [Step 1]
2. [Step 2]
3. Verify by running `[verification command]` — expected output: [expected]

After all steps succeed:
- Store any discovered values: `state.values["[key]"] = [value]`
- Append "phase-1" to `completed_phases` in `state.json`, set `last_updated`
- Print: "✓ Phase 1 complete"

---

## Phase 2: [Phase Name] [ID: phase-2]

Skip if "phase-2" is in `completed_phases`.

1. [Step 1] — you can reference `state.values["[key]"]` from Phase 1 if needed
2. [Step 2]
3. Verify: [verification]

After all steps succeed:
- Append "phase-2" to `completed_phases` in `state.json`, set `last_updated`
- Print: "✓ Phase 2 complete"

---

## Final: Validation [ID: validation]

Verify the end state is fully achieved:

1. [Concrete check 1]
2. [Concrete check 2]
3. [Concrete check 3]

If any check fails: print what failed and the manual fix.

If all pass, print:

```
✅ Setup complete!

What was configured:
  - [item 1]
  - [item 2]

Key values:
  - [Label]: [state.values["key"] or discovered value]

Next steps:
  - [manual action if any]
  - [where to go from here]
```

Update `state.json`: append "validation" to `completed_phases`.
