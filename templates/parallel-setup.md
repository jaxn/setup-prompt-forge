<!--
WORKSPACE SETUP: [Goal Name]
Target: Claude Code (claude-code)
Generated by: setup-prompt-forge

PREREQUISITES:
  - [Prerequisite 1]
  - [Prerequisite 2]

RESUMING: Re-paste this prompt if interrupted. Completed phases will be skipped automatically.
-->

# [Goal Name] — Workspace Setup

You are setting up [brief description]. This setup uses parallel subagents for independent workstreams.

---

## Step 0: Initialize State + TodoList

Set up your TodoList with the following items (one per phase):
- [ ] Phase 0: Preflight Checks
- [ ] Phase 1: [Name]
- [ ] Phase 2: [Name] (parallel)
- [ ] Phase 3: [Name]
- [ ] Validation

Check if `.setup/state.json` exists.

If it does, read it and parse `completed_phases`. Skip any phase whose ID is in this list.

If it does not exist, create `.setup/state.json`:

```json
{
  "goal": "[goal name]",
  "started_at": "[current ISO timestamp]",
  "last_updated": "[current ISO timestamp]",
  "completed_phases": [],
  "values": {},
  "errors": []
}
```

---

## Phase 0: Preflight Checks [ID: preflight]

Skip if "preflight" in `completed_phases`. Mark TodoList item in progress.

Verify each prerequisite. STOP with a clear error message if any fail:

1. [Check 1] — `[command]` → expected: [result]
2. [Check 2] — env var `$[VAR]` must be non-empty
3. [Check 3]

On success:
- Append "preflight" to `completed_phases`, update `last_updated` in `state.json`
- Mark "Phase 0: Preflight" ✓ in TodoList
- Print: "✓ Preflight passed — proceeding to setup"

---

## Phase 1: [Sequential Phase Name] [ID: phase-1]

Skip if "phase-1" in `completed_phases`. Mark TodoList item in progress.

1. [Step 1]
2. [Step 2]
3. Verify: `[command]` → [expected output]

On success:
- Store: `state.values["[key]"] = [discovered value]`
- Append "phase-1" to `completed_phases` in `state.json`
- Mark "Phase 1" ✓ in TodoList

---

## Phase 2: [Parallel Phase Name] [ID: phase-2] [PARALLEL]

Skip if "phase-2" in `completed_phases`. Mark TodoList item in progress.

Spawn the following subagents in parallel. Wait for ALL to complete before continuing.

---

### Subagent A: [Task Name]

You are a subagent performing one part of a workspace setup.

**Your task**: [Clear, self-contained description of what this agent must do]

**Context you have**:
- Working directory: [path]
- [Any other needed info — do NOT say "see state.json"; pass values explicitly]
- Value from earlier phase: `[value]`

**Steps**:
1. [Step 1]
2. [Step 2]
3. Verify: [verification command and expected result]

**When complete**: Write a summary of what you did and any values discovered (e.g., installed path, version) to stdout so the coordinator can collect them.

---

### Subagent B: [Task Name]

You are a subagent performing one part of a workspace setup.

**Your task**: [Clear, self-contained description]

**Context you have**:
- [Everything this agent needs — fully self-contained]

**Steps**:
1. [Step 1]
2. [Step 2]
3. Verify: [verification]

**When complete**: Write a summary with any discovered values to stdout.

---

After both subagents complete, collect their outputs and:
- Store discovered values: `state.values["[key-a]"] = [value-a]`, `state.values["[key-b]"] = [value-b]`
- Verify both expected outputs exist (list them)
- If either failed, print what failed and what the user should do to fix it
- Append "phase-2" to `completed_phases` in `state.json`
- Mark "Phase 2" ✓ in TodoList

---

## Phase 3: [Sequential Phase Using Parallel Results] [ID: phase-3]

Skip if "phase-3" in `completed_phases`. Mark TodoList item in progress.

Use `state.values["[key-a]"]` and `state.values["[key-b]"]` from Phase 2.

1. [Step using collected values]
2. [Step 2]
3. Verify: [verification]

On success:
- Append "phase-3" to `completed_phases` in `state.json`
- Mark "Phase 3" ✓ in TodoList

---

## Final: Validation [ID: validation]

Mark TodoList item in progress.

Verify the complete end state:

1. [Check 1] — `[command]` exits 0
2. [Check 2] — file `[path]` exists and contains `[key]`
3. [Check 3]

If any fail: print the failure and manual remediation steps.

If all pass:

```
✅ Setup complete!

What was configured:
  - [item A from subagent A]
  - [item B from subagent B]
  - [item from phase 3]

Key paths and values:
  - [Label]: [state.values["key"] value]

Manual next steps (if any):
  - [step 1]

State persisted at: .setup/state.json
```

- Append "validation" to `completed_phases` in `state.json`
- Mark "Validation" ✓ in TodoList
